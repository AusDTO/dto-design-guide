<div class="guide-example">
  <h3 class="guide-example--heading">
    <span>Example<span class="visuallyhidden"> of {{ include.title }}</span></span>
  </h3>

    <div class="guide-example--rendered">
  {% include {{ include.markup }} %}
    </div>

  <h3 class="guide-example--heading">
    <span><abbr>HTML</abbr><span class="visuallyhidden"> for {{ include.title }}</span></span>
  </h3>

  <button id="clipboard-{{ include.title | slugify }}" data-clipboard-target="#html-{{ include.title | slugify }}">Copy <abbr>HTML</abbr> to clipboard</button>

  <div class="guide-example--code" id="html-{{ include.title | slugify }}">
    {% highlight html %}{% include {{ include.markup }} %}{% endhighlight %}
  </div>

  <cite class="guide-example--cite">
    Stylesheet source on GitHub:
    <a
      href="{{ site.uikit_github_url }}/blob/master/{{ include.sourceFile }}"
      rel="external">
      <code>{{ include.sourceFile }}</code>
    </a>
  </cite>
</div>


{% comment %}
  This uses clipboard.js v1.5.15, as included minified in _assets/js/.

  For documentation see: https://github.com/zenorocha/clipboard.js

  I’m not sure if we should be including these script tags here for each code
  example include?
{% endcomment %}
<script>
    var clipboard = new Clipboard('#clipboard-{{ include.title | slugify }}');
    clipboard.on('success', function(e) {
        console.log(e);
    });
    clipboard.on('error', function(e) {
        console.log(e);
    });
</script>
