---
general:
  artifacts:
    - "_site"

test:
  override:
    - ./bin/cibuild.sh

deployment:
  development:
    branch: develop
    commands:
      - ./bin/cideploy.sh -develop
  prod:
    branch: master
    commands:
      - ./bin/cideploy.sh
